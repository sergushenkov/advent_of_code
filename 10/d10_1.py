input_string = '''addx 15
addx -11
addx 6
addx -3
addx 5
addx -1
addx -8
addx 13
addx 4
noop
addx -1
addx 5
addx -1
addx 5
addx -1
addx 5
addx -1
addx 5
addx -1
addx -35
addx 1
addx 24
addx -19
addx 1
addx 16
addx -11
noop
noop
addx 21
addx -15
noop
noop
addx -3
addx 9
addx 1
addx -3
addx 8
addx 1
addx 5
noop
noop
noop
noop
noop
addx -36
noop
addx 1
addx 7
noop
noop
noop
addx 2
addx 6
noop
noop
noop
noop
noop
addx 1
noop
noop
addx 7
addx 1
noop
addx -13
addx 13
addx 7
noop
addx 1
addx -33
noop
noop
noop
addx 2
noop
noop
noop
addx 8
noop
addx -1
addx 2
addx 1
noop
addx 17
addx -9
addx 1
addx 1
addx -3
addx 11
noop
noop
addx 1
noop
addx 1
noop
noop
addx -13
addx -19
addx 1
addx 3
addx 26
addx -30
addx 12
addx -1
addx 3
addx 1
noop
noop
noop
addx -9
addx 18
addx 1
addx 2
noop
noop
addx 9
noop
noop
noop
addx -1
addx 2
addx -37
addx 1
addx 3
noop
addx 15
addx -21
addx 22
addx -6
addx 1
noop
addx 2
addx 1
noop
addx -10
noop
noop
addx 20
addx 1
addx 2
addx 2
addx -6
addx -11
noop
noop
noop
'''

with open('input.txt', 'r') as file:
    input_string = file.read()

count_cycle = 20
registr_x = 1
result = 0
check = ['end', 220, 180, 140, 100, 60, 20]

for command in input_string.split('\n'):
    if command == '':
        break
    if command == 'noop':
        count_cycle += 1
        delta = 0
    else:
        delta = int(command[5:])
        count_cycle += 2
    if count_cycle == 39:
        registr_x += delta
        result += registr_x * check.pop()
        count_cycle = -1
        print('a', registr_x, result)
    elif count_cycle == 40:
        result += registr_x * check.pop()
        print('b', registr_x, result)
        registr_x += delta
        count_cycle = 0
    else:
        registr_x += delta

print(result)
